# OpenAI (required)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-5-mini
# Optional: search fallback when mini web_search fails (default gpt-5.5)
# OPENAI_MODEL_SEARCH_FALLBACK=gpt-5.5
# Optional opt-in only — chat defaults to OPENAI_MODEL (gpt-5-mini), not nano:
# OPENAI_MODEL_NANO=gpt-5-nano

PORT=8787
